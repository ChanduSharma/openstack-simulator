[pytest]
testpaths = tests
addopts = -q --strict-markers
# anyio (a Starlette dependency) supplies the async test plugin -- no extra runner needed.
markers =
    slow: exercises a real wall-clock delay
